# The provided code stub reads two integers from input, a and b.
# Add code to print three lines where:
# 1. The first line contains the sum of the two numbers.
# 2. The second line contains the difference of the two numbers (a - b).
# 3. The third line contains the product of the two numbers.

a = int(input("Enter first integer: "))
b = int(input("Enter second integer: "))

print("The sum of 2 numbers: ",a + b)
print("The difference of 2 numbers: ",a - b)
print("The product of 2 numbers: ",a * b)