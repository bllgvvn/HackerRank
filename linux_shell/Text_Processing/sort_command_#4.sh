# You are given a file of text, where each line contains a number.
# There will be no extra characters other than the number or the newline at the end of each line.
# Sort the numbers in descending order - - such that the first line holds the largest number and 
# the last line holds the smallest number.

sort -r -n